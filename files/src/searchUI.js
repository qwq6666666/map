/* ---------------------------------------------------------
   searchUI.js — 組合層（facade）
   ---------------------------------------------------------
   這支檔案不再自己實作搜尋邏輯，改成把拆開後的子模組組合起來，
   並且對外維持跟拆分前完全相同的匯出名稱／使用方式：

     - initSearchUI()：由 main.js 在 loadAppData() 完成後呼叫。

   實際的搜尋流程（候選來源篩選、逐筆圖磚驗證、選取結果的 store
   動作）在 features/search.js；輸入框、按鈕、建議清單、結果面板
   等 DOM 相關邏輯在 ui/search.js。
--------------------------------------------------------- */
export { initSearchUI } from './ui/search.js';
